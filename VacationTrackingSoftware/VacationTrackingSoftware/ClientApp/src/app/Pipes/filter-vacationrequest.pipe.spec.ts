import { FilterVacationRequestPipe } from './filter-vacationrequest.pipe';

describe('FilterVacationRequestPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVacationRequestPipe();
    expect(pipe).toBeTruthy();
  });
});
